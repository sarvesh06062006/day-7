#include <stdio.h>

int main() {
    int n,i;
    scanf("%d", &n);

    int arr[n];
    int sum=0;
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        sum+=arr[i];
    }

    for (int i = 0; i < n; i++) {
        printf("%d ",sum-arr[i]);
    }
    return 0;
}
